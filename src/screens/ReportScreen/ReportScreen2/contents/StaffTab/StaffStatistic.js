import React, { useState, useRef, useCallback } from "react";
import { View } from "react-native";

export default function StaffStatistic({ style, onGoSalary }) {
  return <View style={{ backgroundColor: "red", flex: 1 }}></View>;
}
