import {
    StyleSheet,
    Platform
} from 'react-native';

import { scaleSize } from '@utils';

export default StyleSheet.create({
    container: {
        flex: 1,
        // backgroundColor:'red'
    },
})