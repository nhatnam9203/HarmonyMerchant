import { CodePushContext } from '@shared/providers/CodePushProvider';
import React from 'react';

export const useProps = (_params) => {
  // const { codePushCheck } = React.useContext(CodePushContext);

  // React.useEffect(() => {
  //   codePushCheck();
  // }, []);
  return {};
};
